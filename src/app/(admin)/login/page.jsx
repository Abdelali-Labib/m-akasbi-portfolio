import LoginClient from "./LoginClient";

/**
 * Login page component - Server-side component that renders the login form.
 * Authentication and client-side logic are handled in LoginClient.
 */
export default function LoginPage() {
  return <LoginClient />;
}
