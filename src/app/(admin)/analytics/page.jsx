import GoogleAnalyticsDashboard from '@/components/admin/GoogleAnalyticsDashboard';

/**
 * Analytics page component - Server-side component that renders the Google Analytics dashboard.
 * Authentication is handled at the layout level.
 */
export default function AnalyticsPage() {
  return <GoogleAnalyticsDashboard />;
}
